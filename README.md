Скрипт для отслеживания появления новых записей в списках умерших на страницах Википедии
и формирования уведомлений с краткой информацией о человеке.

## 📌 Описание задачи

На страницах Википедии существуют списки умерших по месяцам (например, "Умершие в августе 2023 года"),
которые со временем дополняются.

Необходимо:
- периодически проверять страницу
- определять появление новых записей
- для каждой новой записи получать ссылку на русскоязычную статью (или fallback-ссылку)
- извлекать первый абзац статьи без ссылок, спецсимволов и ударений
- отправлять уведомление (в проекте используется mock-режим)

---

## ⚙️ Основные возможности

- Парсинг HTML-страницы Википедии
- Устойчивость к изменениям вёрстки
- Корректная обработка inline-ссылок
- Хранение состояния между запусками
- Polling без cron и внешних планировщиков
- Mock-уведомления в консоль (готово к подключению SMTP)

---

## 📦 Установка:
### 1. Клонируй репозиторий и перейди в папку проекта
```bash
git clone https://github.com/Xlnt777/MAST_Test.git
```
### 2. Создай и активируй виртуальное окружение
```bash
python -m venv venv
source venv/bin/activate   # Linux / Mac
venv\Scripts\activate      # Windows
```
### 3. Установить зависимости с помощью pip:
```bash
pip install -r requirements.txt
```
### 4. Запуск скрипта:
```bash
python main.py
```

---

## 🔔 Уведомления
В проекте используется mock-режим уведомлений сообщения выводятся в консоль.
SMTP-отправка может быть подключена при необходимости через config.py.

## 🧠 Принцип работы
 - При первом запуске все текущие записи считаются обработанными.
 - Имена сохраняются в data/sent.json.
 - Скрипт периодически опрашивает страницу.
 - При появлении нового имени формируется уведомление ровно один раз.

## 📌 Примечания
 - Скрипт не использует cron или сторонние планировщики.
 - Обработка сетевых ошибок не приводит к остановке сервиса.
 - Проект ориентирован на простоту запуска и демонстрацию логики.
